<h1>追加でこれをやれ</h1>

<%= form_with(model: @task, local: true) do |f| %>

  
<%= f.label :content, 'やること' %>

  
<%= f.text_field :content %>


  
<%= f.submit 'やれ' %>

<% end %>


<%= link_to '一覧', tasks_path %>
